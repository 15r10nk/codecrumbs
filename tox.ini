# For more information about tox, see https://tox.readthedocs.io/en/latest/
[tox]
isolated_build=True
envlist = py38,py39,py310

[testenv]
deps = pytest>=3.0
commands = pytest --assert=plain {posargs:tests}

